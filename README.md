client 
run cmd :(client) npm run dev

sever 
run cmd :(server) nodemon index.js

before running te project :
 add .env file with:
 MONGODB_URL=""
 PORT =1080
 JWT_SECRET_KEY=
 JWT_EXPIRES_IN =7d
 GOOGLE_CLIENT_ID=
GOOGLE_SECRET=
